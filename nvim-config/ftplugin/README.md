# Filetype Plugins

This directory contains Neovim filetype-specific configurations.
Files named `{filetype}.lua` are automatically sourced when editing files of that type.

Example: `python.lua` would be sourced when editing Python files.
